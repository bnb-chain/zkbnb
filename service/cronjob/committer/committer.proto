syntax = "proto3";

package committer;

option go_package = "./committerProto";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Committer {
  rpc Ping(Request) returns(Response);
}
