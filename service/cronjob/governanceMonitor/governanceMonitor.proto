syntax = "proto3";

package governanceMonitor;

option go_package = "./governanceMonitor";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service GovernanceMonitor {
  rpc Ping(Request) returns(Response);
}
