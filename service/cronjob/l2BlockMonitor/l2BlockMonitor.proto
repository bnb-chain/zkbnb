syntax = "proto3";

package l2BlockMonitor;

option go_package = "./l2BlockMonitor";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service L2BlockMonitor {
  rpc Ping(Request) returns(Response);
}
