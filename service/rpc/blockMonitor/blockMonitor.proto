syntax = "proto3";

package blockMonitor;

option go_package = "./blockmonitor";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service BlockMonitor {
  rpc Ping(Request) returns(Response);
}
